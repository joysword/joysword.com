[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ynab-importer"
version = "0.1.0"
description = "Import bank CSV transactions into YNAB"
requires-python = ">=3.10"
dependencies = [
    "click>=8.0",
    "pydantic>=2.0",
    "pyyaml>=6.0",
    "watchdog>=3.0",
    "requests>=2.28",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-mock>=3.0",
]

[project.scripts]
ynab-importer = "ynab_importer.cli:cli"

[tool.setuptools.packages.find]
where = ["src"]
